# simple-QR-code-generator_scanner
Few line of code for qr code generation and scanning.

You also need to install pyqrcode,pyzbar , pillow, pypng:
pip install pyqrcode
pip install pyzbar
pip install pillow
pip install pypng
